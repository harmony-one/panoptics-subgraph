{
  "name": "panoptics-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ panoptics-subgraph",
    "create-local": "graph create --node http://localhost:8020/ panoptics-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ panoptics-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 panoptics-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.83.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.29",
    "matchstick-as": "0.5.0"
  }
}